interface:
  display_name: "NestJS Vitest Setup"
  short_description: "Deterministically install, migrate, and verify Vitest in NestJS backends"
  default_prompt: "Use $nestjs-vitest-setup to add or repair Vitest in this NestJS project, migrate Jest usage, and verify test, e2e, and coverage gates."
