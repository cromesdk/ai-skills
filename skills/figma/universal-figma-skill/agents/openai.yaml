interface:
  display_name: "Universal Figma Workflow"
  short_description: "Run a single deterministic Figma-to-Angular 20 workflow for scoped UI delivery."
  default_prompt: "Implement or refactor the requested Figma-driven Angular 20 scope end to end: confirm inputs, map to Atomic Design, reuse existing components/tokens, enforce feature-first folders and component companion files, apply Lucide bridging only when confidently mapped and already installed, then run targeted verification and report outcomes."
