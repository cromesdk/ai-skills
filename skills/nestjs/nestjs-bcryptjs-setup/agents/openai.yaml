interface:
  display_name: "NestJS bcryptjs Setup"
  short_description: "Install, repair, and verify NestJS bcryptjs hash/verify wiring with SALT_ROUNDS validation"
  default_prompt: "Use $nestjs-bcryptjs-setup to add or fix bcryptjs in this NestJS backend, centralize hashing in EncryptionService and EncryptionModule, validate SALT_ROUNDS via ConfigModule, and verify build/test plus hash/compare behavior."
