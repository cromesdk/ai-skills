interface:
  display_name: "Spec Kit Setup"
  short_description: "Deterministic spec-kit release-based install/init/repair with explicit --ai selection and safe reconcile defaults"
  default_prompt: "Run a deterministic github/spec-kit setup workflow: execute fail-fast preflight checks (git, gh, gh auth, uv, python), classify repository state, choose fresh install or safe non-destructive reconcile, install/upgrade from https://github.com/github/spec-kit/releases by downloading and extracting the correct artifact for OS/architecture/engine, initialize with explicit --ai <assistant> and --here (plus --script ps in PowerShell), verify .specify and .github/prompts readiness, and return exact next-step guidance for /constitution, /specify, /plan, and /tasks."
