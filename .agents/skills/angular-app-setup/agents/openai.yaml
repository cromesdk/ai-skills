interface:
  display_name: "Angular 20 App Setup"
  short_description: "Scaffold Angular 20 in the current folder with strict defaults and safety checks"
  default_prompt: "Use $angular-app-setup to create an Angular 20 app in the current directory with --directory ., strict defaults, preflight safety checks, and build/test verification."
